generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Character {
  id            String        @id @default(cuid())
  type          String        @unique // 'josh' or 'abby' - 现在是唯一的
  level         Int           @default(1)
  experience    Int           @default(0)
  expToNextLevel Int          @default(100)
  eth           String        // Store bigint as string
  
  // Base stats
  baseHp        Int           @default(100)
  baseMp        Int           @default(50)
  baseAttack    Int           @default(20)
  baseDefense   Int           @default(10)
  
  // Inventory (JSON array of items)
  inventory     String        @default("[]")
  
  // Equipment IDs
  weaponId      String?
  armorId       String?
  shieldId      String?
  
  // Progress tracking
  currentStageId String?
  stagesCleared  String        @default("[]") // JSON array of cleared stage IDs
  totalEnemiesDefeated Int    @default(0)
  totalQuestionsAnswered Int   @default(0)
  totalCorrectAnswers Int     @default(0)
  
  // Owned equipment tracking
  ownedEquipment String        @default("[]") // JSON array of owned equipment IDs
  stagesPaidFor  String        @default("[]") // JSON array of stage IDs that have been paid for
  
  weapon        Equipment?    @relation("CharacterWeapon", fields: [weaponId], references: [id])
  armor         Equipment?    @relation("CharacterArmor", fields: [armorId], references: [id])
  shield        Equipment?    @relation("CharacterShield", fields: [shieldId], references: [id])
  
  createdAt     DateTime      @default(now())
  updatedAt     DateTime      @updatedAt
  
  // 添加备份关系
  backups       CharacterBackup[]
}

model Equipment {
  id            String        @id @default(cuid())
  name          String
  type          String        // 'weapon', 'armor', 'shield'
  tier          Int           // 1-7 for quality
  price         String        // Store bigint as string
  levelRequirement Int       @default(1)
  
  // Stat bonuses
  hpBonus       Int           @default(0)
  attackBonus   Int           @default(0)
  defenseBonus  Int           @default(0)
  
  // Visual
  icon          String        // emoji or image path
  description   String
  
  // Relations
  charactersWithWeapon Character[] @relation("CharacterWeapon")
  charactersWithArmor  Character[] @relation("CharacterArmor")
  charactersWithShield Character[] @relation("CharacterShield")
  
  createdAt     DateTime      @default(now())
}

model Stage {
  id            String        @id @default(cuid())
  name          String
  area          String        // e.g., "森林", "沙漠", "火山"
  difficulty    Int           // 1-10
  levelRequirement Int
  enemies       String        // JSON array of enemy configurations
  ethReward     String        // Store bigint as string
  expReward     Int
  description   String
  icon          String
  order         Int           // for sorting stages
  
  createdAt     DateTime      @default(now())
}

model CharacterBackup {
  id            String        @id @default(cuid())
  characterType String        // 'josh' or 'abby'
  backupName    String
  backupData    String        // JSON string of full character data
  createdAt     DateTime      @default(now())
  
  character     Character?    @relation(fields: [characterType], references: [type])
  
  @@index([characterType])
}